extends ../templates/template

block header
    include ../includes/header-menu
    block menu-phone
        include ../includes/header-menu-phone

block menu-scroll
    include ../includes/menu-scroll

block main
    .grupo-muebles
        h2.grupo-muebles__title Estantes
        p.grupo-muebles__content Tenemos estantes de diferentes colores y tamaños de diversos materiales en melamina y MDf

    .main__container-index
        //- ============================================
        - var ventaindex = true
        - var dirIMG = 'estante/'
        - var codigo = 'est001'
        - var title = 'Estante de melamine para libros'
        - var precio = 'S/ 90'
        - var color = false
        - const est001a = {ventaindex, dirIMG, codigo, title, precio, color}
        +product (est001a)
        //- ============================================
        - var ventaindex = true
        - var dirIMG = 'estante/'
        - var codigo = 'est001'
        - var title = 'Estante de melamine para libros'
        - var precio = 'S/ 90'
        - var color = false
        - const est002a = {ventaindex, dirIMG, codigo, title, precio, color}
        +product (est002a)

        //- include ../includes/main-index


block footer
    include ../includes/footer